## Profiling Code

While profiling the firmware with simple get_time() before and after a
function, I noticed that sensor readings are very similar between runs; they
change of just ~100us. Instead writing to flash through LittleFS changes a lot.

AI, after reading the data, says:
So 30ms is probably the easy path. 60ms is a normal metadata/commit path. 200ms
is likely a block erase, compaction, or more expensive LittleFS sync path.
