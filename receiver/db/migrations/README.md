# Migration scripts

Only immutable, four-digit-prefixed `.sql` migration files belong here. The
application runner ignores this README and owns all transaction boundaries.
See [`../README.md`](../README.md) for the migration contract.
