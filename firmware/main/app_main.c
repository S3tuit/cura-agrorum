/*
 * Fresh entry point for the LoRa v2 firmware. Wake-cycle orchestration will be
 * implemented from ARCHITECTURE.md; returning leaves the ESP-IDF idle task in
 * control without retaining any behavior from the legacy Wi-Fi firmware.
 */
void app_main(void) {}
